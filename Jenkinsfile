pipeline {
    agent any
    tools{
        nodejs 'nodejs16.16'
    }

    stages {
        stage('Build') {
            steps {
                sh "npm install"
                sh "npm run "
            
            }
        }
    }
}
